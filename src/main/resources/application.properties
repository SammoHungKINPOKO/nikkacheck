# 開発環境を使用する
spring.profiles.active=dev

# 本番環境を使用する
# spring.profiles.active=prd

# =============================================================================
# DB
# =============================================================================

# spring.jpa.hibernate.ddl-auto can be none, update, create, create-drop, refer to the Hibernate documentation for details.
# none This is the default for MySQL, no change to the database structure.
# update Hibernate changes the database according to the given Entity structures.
# create Creates the database every time, but don’t drop it when close.
# create-drop Creates the database then drops it when the SessionFactory closes.
spring.jpa.hibernate.ddl-auto=none

#spring.datasource.url=jdbc:mysql://localhost:3306/nikkacheck
spring.datasource.url=jdbc:mysql://192.168.11.9:3306/
#spring.datasource.username=root
spring.datasource.username=m4usta13ng
#spring.datasource.password=root
spring.datasource.password=password